#include "breadthFirstSearch.hpp"


namespace breadthFirst {
    
    template <typename T>
    void search(AdjacencyList graph, T start, T target)
    {
        bool visited[graph] = { 0 };
    }
    
}
